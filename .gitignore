node_modules
dist
dist-ssr
*.local
.DS_Store
*.tsbuildinfo
.mcp.json
# local env — never commit real keys
.env
.env.local
.env.*.local

# turborepo
.turbo

# vitest coverage is reported in the terminal; nothing should be written to disk
coverage/
