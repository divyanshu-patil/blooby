name: Keep Render Alive

on:
  schedule:
    - cron: "7,21,35,49 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Render
        run: |
          curl -fsS --max-time 30 "${{ secrets.API_URL }}"
