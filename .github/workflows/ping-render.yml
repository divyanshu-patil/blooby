name: Keep Render Alive

on:
  schedule:
    - cron: "*/3 * * * *" # Every 3 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render
        run: |
          curl -I  "${{ secrets.API_URL }}"
